# README
Prerequisites
  Ruby 3.1.0
  SQLite3


Development Setup
  Clone the repo
    `git clone `
  Install dependencies and prepare database
    `yarn install`
    `./bin/setup`
  Start server
    `./bin/dev`
